<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_i2i_export_wizard_form" model="ir.ui.view">
            <field name="name">i2i.export.wizard.form</field>
            <field name="model">i2i.export.wizard</field>
            <field name="arch" type="xml">
                <form string="Export I2I Items">
                    <sheet>
                        <group>
                            <group string="Filters">
                                <field name="project_id"/>
                                <field name="state_filter"/>
                                <field name="date_from"/>
                                <field name="date_to"/>
                            </group>
                            <group string="Output">
                                <field name="file_format"/>
                            </group>
                        </group>
                        <group string="Result" invisible="not row_count">
                            <field name="row_count" readonly="1"/>
                            <field name="exported_filename" readonly="1"/>
                            <field name="exported_file" filename="exported_filename" readonly="1"/>
                        </group>
                    </sheet>
                    <footer>
                        <button name="action_export" type="object" string="Export" class="btn-primary"/>
                        <button string="Close" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_i2i_export_wizard" model="ir.actions.act_window">
            <field name="name">Export I2I Items</field>
            <field name="res_model">i2i.export.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
