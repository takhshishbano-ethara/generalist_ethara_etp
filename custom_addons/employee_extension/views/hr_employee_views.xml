<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
        Employee ID display intentionally lives in the employee_role_import
        module, which defines the canonical `employee_code` field (populated by
        the CSV import) and loads AFTER this module. The deprecated `emp_id`
        field defined here is no longer shown anywhere; see
        employee_role_import/views/hr_employee_views.xml for the form/list/search
        Employee ID columns.
    -->

</odoo>
