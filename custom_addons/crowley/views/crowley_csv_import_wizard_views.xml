<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_crowley_csv_import_wizard_form" model="ir.ui.view">
            <field name="name">crowley.csv.import.wizard.form</field>
            <field name="model">crowley.csv.import.wizard</field>
            <field name="arch" type="xml">
                <form string="Import CSV">
                    <group invisible="state != 'draft'">
                        <field name="csv_file" filename="csv_filename"/>
                        <field name="csv_filename" invisible="1"/>
                        <field name="state" invisible="1"/>
                    </group>
                    <group invisible="state != 'done'">
                        <field name="state" invisible="1"/>
                        <group>
                            <field name="import_count"/>
                            <field name="skip_count"/>
                            <field name="error_count"/>
                        </group>
                        <field name="log_text" nolabel="1" colspan="2" readonly="1"/>
                    </group>
                    <footer>
                        <button string="Import"
                                name="action_import"
                                type="object"
                                class="btn-primary"
                                invisible="state != 'draft'"/>
                        <button string="Close"
                                class="btn-secondary"
                                special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_crowley_csv_import_wizard" model="ir.actions.act_window">
            <field name="name">Import Generations (CSV)</field>
            <field name="res_model">crowley.csv.import.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
