# aurora_extension defines no models of its own — it is an API-only extension
# whose endpoints compute live from aurora.evaluation.instance and serve the
# auto-created Aurora project (see data/aurora_project_data.xml).
