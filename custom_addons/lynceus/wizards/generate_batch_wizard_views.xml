<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_lynceus_generate_batch_wizard_form" model="ir.ui.view">
        <field name="name">lynceus.generate.batch.wizard.form</field>
        <field name="model">lynceus.generate.batch.wizard</field>
        <field name="arch" type="xml">
            <form string="Generate Lynceus Batch">
                <sheet>
                    <group>
                        <field name="target_n"/>
                    </group>
                    <div class="alert alert-info" role="alert">
                        Each call to Anthropic returns exactly one prompt (per seed-prompt rule).
                        Lynceus will SHA256-deduplicate every output against the History Registry
                        and keep retrying with fresh SEED= until N unique prompts enter the pool.
                        Dedup rejection rate grows as the pool matures - watch the batch stats.
                    </div>
                </sheet>
                <footer>
                    <button name="action_generate"
                            string="Generate"
                            type="object"
                            class="btn-primary"/>
                    <button string="Cancel"
                            class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_lynceus_generate_batch_wizard" model="ir.actions.act_window">
        <field name="name">Generate Batch</field>
        <field name="res_model">lynceus.generate.batch.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
