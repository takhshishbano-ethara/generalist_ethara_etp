<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_ethara_project_budget_topup_list" model="ir.ui.view">
        <field name="name">ethara.project.budget.topup.list</field>
        <field name="model">ethara.project.budget.topup</field>
        <field name="arch" type="xml">
            <list string="Budget Top-ups">
                <field name="name"/>
                <field name="budget_id"/>
                <field name="ethara_project_id"/>
                <field name="reason_id" optional="show"/>
                <field name="amount"/>
                <field name="requester_id"/>
                <field name="approver_id" optional="show"/>
                <field name="create_date"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'draft'"
                       decoration-warning="state == 'pending'"
                       decoration-success="state == 'approved'"
                       decoration-danger="state == 'rejected'"/>
            </list>
        </field>
    </record>

    <record id="view_ethara_project_budget_topup_form" model="ir.ui.view">
        <field name="name">ethara.project.budget.topup.form</field>
        <field name="model">ethara.project.budget.topup</field>
        <field name="arch" type="xml">
            <form string="Budget Top-up">
                <header>
                    <button name="action_submit" type="object"
                            string="Submit for Approval"
                            class="oe_highlight"
                            invisible="state != 'draft'"/>
                    <button name="action_approve" type="object"
                            string="Approve"
                            class="oe_highlight"
                            invisible="state != 'pending'"/>
                    <button name="action_reject" type="object"
                            string="Reject"
                            invisible="state != 'pending'"/>
                    <button name="action_reset_to_draft" type="object"
                            string="Reset to Draft"
                            invisible="state != 'rejected'"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,pending,approved"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="budget_id"
                                   options="{'no_create': True}"/>
                            <field name="ethara_project_id" readonly="1"/>
                            <field name="reason_id"
                                   options="{'no_create_edit': True}"/>
                            <field name="amount"/>
                        </group>
                        <group>
                            <field name="requester_id" readonly="1"/>
                            <field name="approver_id" readonly="1"/>
                            <field name="approval_date" readonly="1"/>
                        </group>
                    </group>
                    <group>
                        <field name="justification"/>
                    </group>
                    <group string="Rejection" invisible="not rejection_reason">
                        <field name="rejection_reason" readonly="1" nolabel="1"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_ethara_project_budget_topup" model="ir.actions.act_window">
        <field name="name">Budget Top-ups</field>
        <field name="res_model">ethara.project.budget.topup</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
