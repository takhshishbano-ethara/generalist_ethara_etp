<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Root Menu -->
    <menuitem id="commit0_menu_root"
        name="Kaiju"
        web_icon="commit0_pipeline,static/description/icon.png"
        sequence="100"
        groups="commit0_pipeline.group_commit0_user"/>

    <!-- Pipeline Runs -->
    <menuitem id="commit0_menu_runs"
        name="Pipeline Runs"
        parent="commit0_menu_root"
        action="commit0_pipeline_run_action"
        sequence="10"/>

    <!-- Repository Status -->
    <menuitem id="commit0_menu_repos"
        name="Repository Status"
        parent="commit0_menu_root"
        action="commit0_repo_entry_action"
        sequence="20"
        groups="commit0_pipeline.group_commit0_admin"/>

    <!-- Discovery Candidates -->
    <menuitem id="commit0_menu_discovery"
        name="Discovery Candidates"
        parent="commit0_menu_root"
        action="commit0_discovery_candidate_action"
        sequence="30"
        groups="commit0_pipeline.group_commit0_admin"/>

    <!-- ================================================================== -->
    <!-- Repo Checker Section                                               -->
    <!-- ================================================================== -->
    <menuitem id="commit0_menu_repo_checker"
        name="Repo Checker"
        parent="commit0_menu_root"
        sequence="40"/>

    <menuitem id="commit0_menu_evaluations"
        name="Evaluations"
        parent="commit0_menu_repo_checker"
        action="commit0_repo_evaluation_action"
        sequence="10"/>

    <!-- Configuration submenu -->
    <menuitem id="commit0_menu_config"
        name="Configuration"
        parent="commit0_menu_root"
        sequence="90"
        groups="commit0_pipeline.group_commit0_admin"/>

    <menuitem id="commit0_menu_settings"
        name="Settings"
        parent="commit0_menu_config"
        action="base_setup.action_general_configuration"
        sequence="10"/>
</odoo>
