<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Root Menu -->
    <menuitem id="commit0_menu_root"
        name="Kaiju"
        web_icon="commit0_pipeline,static/description/icon.png"
        sequence="100"
        groups="commit0_pipeline.group_commit0_user"/>

    <!-- Kaiju Tasks (primary entry point) -->
    <menuitem id="commit0_menu_tasks"
        name="Tasks"
        parent="commit0_menu_root"
        action="commit0_repo_evaluation_action"
        sequence="10"/>

    <!-- Configuration submenu -->
    <menuitem id="commit0_menu_config"
        name="Configuration"
        parent="commit0_menu_root"
        sequence="90"
        groups="commit0_pipeline.group_commit0_admin"/>

    <menuitem id="commit0_menu_settings"
        name="Settings"
        parent="commit0_menu_config"
        action="base_setup.action_general_configuration"
        sequence="10"/>
</odoo>
