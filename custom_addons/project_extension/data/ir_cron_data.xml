<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <record id="ir_cron_escalate_project_blockers" model="ir.cron">
            <field name="name">Project Blocker: Escalation Checker</field>
            <field name="model_id" ref="model_project_blocker"/>
            <field name="state">code</field>
            <field name="code">model._cron_escalate_blockers()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="nextcall" eval="(DateTime.now() + timedelta(hours=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
            <field name="active" eval="True"/>
            <field name="priority">5</field>
        </record>

    </data>
</odoo>
