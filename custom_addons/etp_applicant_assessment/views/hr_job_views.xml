<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_hr_job_form_applicant_assessment" model="ir.ui.view">
        <field name="name">hr.job.form.applicant.assessment</field>
        <field name="model">hr.job</field>
        <field name="inherit_id" ref="hr.view_hr_job_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button
                    name="action_view_assessments"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-graduation-cap"
                    invisible="assessment_count == 0">
                    <field name="assessment_count" widget="statinfo" string="Assessments"/>
                </button>
            </xpath>
            <xpath expr="//sheet" position="inside">
                <group string="Applicant Assessment" name="applicant_assessment_group">
                    <field name="assessment_template_id"
                           options="{'no_create_edit': True}"
                           domain="[('active','=',True)]"/>
                    <field name="assessment_auto_send"
                           invisible="not assessment_template_id"/>
                </group>
            </xpath>
        </field>
    </record>

</odoo>
